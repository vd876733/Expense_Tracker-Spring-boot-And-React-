# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# Application Name
spring.application.name=Personal Finance Tracker

# Database Configuration (Neon DB)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-ancient-flower-a7o6ufvg-pooler.ap-southeast-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_LHuKMz2FxC6R}

# HikariCP Settings
spring.datasource.hikari.max-lifetime=1680000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.validation-timeout=3000

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.org.hibernate.envers.audit_table_suffix=_aud

# JWT Configuration
app.jwtSecret=${JWT_SECRET:MDEyMzQ1Njc4OUFCQ0RFRjAxMjM0NTY3ODlBQkNERUY=}
app.jwtExpirationMs=86400000

# CORS Configuration (Updated with your exact Vercel frontend domain)
allowed.origins=${ALLOWED_ORIGINS:https://expense-tracker-spring-boot-and-rea.vercel.app,http://localhost:3000}

# Google OAuth Configuration
app.googleClientId=${GOOGLE_CLIENT_ID:884510669054-acldripspk9ucf1kp50ad5qlv2l0fv6a.apps.googleusercontent.com}

# Gemini AI Configuration
gemini.api.key=${GEMINI_API_KEY:}
gemini.model=gemini-pro

# Mail Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Twilio SMS Configuration
twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.phone-number=${TWILIO_PHONE_NUMBER:}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.root=INFO
logging.level.com.financetracker=INFO
